# maple 開發日誌與架構（供接續）

手機遠端遊玩 + 自動巡邏掛機系統。

## 架構
- **後端** FastAPI（`server/`），token=`***REMOVED***`（MAPLE_TOKEN）。訪客短密碼限 4/←/→。
- **輸入**：Arduino HID 鍵盤（`press`=key_down→hold~60ms→key_up，**不用 tap，遊戲會漏讀**）+ km.dll 滑鼠。
- **畫面**：WGC 視窗捕獲 maplestory.exe（只讀屏、不碰遊戲記憶體、不注入）。
- **重啟**：`restart-admin.bat`（UAC 靜默提權，載入最新碼）。改後端必重啟；改前端硬刷新。
- 主要檔案：`main.py`(端點/lifespan)、`navigator.py`(導航/巡邏)、`pathgraph.py`(路徑圖)、`mapdata.py`(座標/平台/繩索/profile)、`minimap.py`(小地圖偵測)、`idle_mode.py`(隨機AFK)、`web/index.html`(中控頁,單檔)。

## 前端結構（index.html）
儀表板 4 分頁 tab：🏠出租 / 🤖閒置 / 🗺巡邏 / 🎮遠端。共用底部=解除卡死+登出。
- 巡邏 tab：頂部=開始/停止巡邏+狀態(phase)+放置技能倒數監控；下方「巡邏設定」=方向/小地圖預覽/地圖座標/地形/平A/放置技能。
- 遙控漢堡「操作」區有「停止巡邏」。
- 浮動 toast（`toast(msg,ok)`，頂部居中，2.5s）：凡走 mapAct 或巡邏開關都彈綠(成功)/紅(失敗)。
- 按鍵一律虛擬鍵盤 `openKeyPicker` 點選（#picker，已移到 body 頂層避 stacking context 被 dashView 蓋）。
- 小地圖預覽疊加：平台=藍線、繩索=黃虛線、記錄點=橘點+青圈(攻擊範圍)、**當前導航路線=青虛線+節點圓點**(每秒讀 /nav/status 的 path)。

## 移動模型（實測標定，關鍵資產）
- **黃點靜止 0px 抖動**（40/40 全同）→ 判定要 `_settle` 等停穩再讀，移動中瞬時值不可靠。
- **二段跳**：同層水平**精確 30px**（16組±1，左右對稱）；落更低平台時抛物線飛更遠（實測 37px @ 下降 8px）。X→interval0.2→P。
- **走路脈衝**：0.05~0.08s→1px、0.10s→0~2px、0.13s→3px、0.20s→4px（小地圖解析度粗、慣性極小、松鍵即停）。
- **繩索**：站到繩索 x 按 C 一路上到頂平台（如 x28：y68→y34，dy-34）；不在繩索 x 原地按 C 無效。上到頂再下跳修正到目標層。
- **下跳**：垂直落到正下方平台（走出/跳出邊緣也會掉落，可當下降手段）。
- 座標系=小地圖黃點 (x,y)，記錄點/平台/繩索/紫標同一系。

## pathgraph（路徑規劃）
`build_physics(points, platforms, jump=30, jump_up=11)`：只用平台+落點幾何建圖。
- 節點=記錄點+連接點；邊：walk(同平台)、jump(端點朝左右飛30落到落點平台)、fall(正下方平台垂直落)、rope(大落差>jump_up 上升)。
- `shortest_path` Dijkstra、`nearest_node` 當前位置對應節點。
- 繩索零標記——靠平台重疊/幾何推，繩索確切 x 執行時探。
- 舊版 `build_overlap`（重疊+接近跨間隙）保留。

## navigator（按鍵流程）
- `_goto_sync(tx,ty,skills,precise)`：有平台(terrain_fn)→`_goto_via_graph`(圖導航)；無→簡單導航(fallback)。
- `_goto_via_graph`：沿 path 分段 walk/jump/rope/fall + 黃點閉環。**jump 段算精確起跳點**（使落點=起跳±30 落在目標平台內、取範圍中間避邊緣，`_walk_to` 精確走到再二段跳）。
- `_fine_tune_x`：精確點停穩(`_settle`)+分檔脈衝回正到 ±1px（實測偏移8px→4~5步收斂）。
- **移動攻擊**(平A mode="move")：走路(walk)`_same_skills` 按住攻擊鍵；二段跳/下跳/繩索/到點 `_release_atk` 放開。勾選「二段跳保持攻擊」`_jump_hold_atk`、「下跳保持攻擊」`_fall_hold_atk`(平A設定,存 attack.jump_atk/fall_atk)。
- terrain 於導航啟動(靜止)取一次(current_map_id 移動中偶爾 None)；`_state["terr_n"]` 診斷。
- **移動數據記錄** `nav_moves.jsonl`：每段 {t,mode,act,start,target,end}，供訓練/調參。

## 巡邏（_patrol_run）
- 每輪 `points_fn()` 重讀最新點（運行中改設定即時生效）。**順序完全隨機、不連號**。
- 選點優先序：①冷卻好的「導航略過」放置技能點→優先去放 ②普通點(+非略過技能點)不連號循環 ③只剩冷卻中略過點→等待。
- 到點：先平A(確保落地)→放置技能(冷卻好才放、可精確、可略過；安全停攻擊0.45s再放)。**未到達(arrived=False)不施放、重試**。
- 放置技能：每點 {skill,cd,precise,skip}；`_place` 冷卻表，/nav/status 回 placements 倒數。
- **紫標(rune)出現**(minimap 事件 hook + 每輪兜底) + Telegram 通知 → 開了自動解除就去解符文,否則暫停巡邏(見下)。

## mapdata / 端點
- point={x,y,skill,cd,precise,skip}；平台={y,xA,xB}；繩索={x}；平A attack={key,mode,jump_atk,fall_atk}(存 layouts/_attack.json)。
- profile(具名設置「北方」等)存 profiles/<name>.json：points+platforms+ropes+attack。**load_profile 已按尺寸縮放座標**(當前map_id÷來源尺寸)。
- 端點：/map/{status,record,remove_last,clear,attack,point_skill,platform/*,rope/*,profile/*}、/nav/{move_to,patrol,status,stop}、/idle/*。

## ⚠️ 已知重大問題
1. **小地圖偵測尺寸不穩定**（同地圖出 165x84 / 170x111 / 330x168）。165→330 是精確2倍(解析度/DPI)，170x111 是框選含邊框差異(比例不一致)。導致 map_id 變、座標錯位。load_profile 縮放只救等比；**根治要小地圖偵測每次框選同一區域(邊框配對在不同解析度/DPI 邊界不一致)**。minimap 記憶：邊框線配對法可靠、底色/輪廓不可靠。
2. 閒置模式曾誤加 has_layout 檢查(已移除，閒置=純隨機不需座標)。

## ✅ 符文(紫方塊 rune)自動解除 —— 常駐 claude CLI 當辨識器(**實機解除成功**)
**第一約束是延遲：謎題窗只有 10~14 秒**(使用者實測)。前三次嘗試都死在辨識這一段：
1. **auto-maple TF 模型 —— 死路**：官方 Google Drive 分享夾缺權重大檔
   `variables.data-00000-of-00001`(只有 18KB 的 `variables.index`；`checkpoint/ckpt-0.data`
   也缺)，GitHub issue #211 多人同症無解、無鏡像。純計算圖 saved_model.pb(4.8M)無權重無法推理。
2. **傳統 CV(HSV 濾色 + 連通塊 + 質心偏移判向) —— 不穩**：箭頭疊在遊戲背景上，濾色遮罩
   雜訊多、常抓不滿 4 支；箭頭非實心三角時質心法判向失準。
3. **本機 VLM(Qwen2.5-VL + `venv-vlm` 4.7GB torch cu128) —— 太慢**：單次 ~7s，且拖進
   獨立子行程管理。已全數刪除(含 venv-vlm、rune_refs 參考截圖)。

**現行方案：`server/rune.py` 呼叫常駐 claude CLI**。實測數據(合成 4 箭頭測試圖，全對)：

| 方式 | 耗時 |
|---|---|
| `claude -p` 每次重開行程 | 6.3 / 6.6 / 7.7s |
| 同上但先裁切成 520x200 | 6.6 / 7.0s ← **裁切完全不影響速度** |
| CLI 純開銷(無工具無圖) | **3.4s** ← 瓶頸是 Node 啟動 + Read 工具多一次模型往返 |
| 常駐 `--input-format stream-json` 第 1 次 | 6.3 / 7.2s |
| 常駐後續訊息 | **2.2 / 2.4 / 4.8s** ← 採用 |

- **關鍵設計**：worker 開起來就不關(`_Worker`，stream-json 雙向 + reader thread 讓 ask 能設逾時)，
  並在「走去符文」之前先 `worker_warmup()` 預熱，把 5~7s 冷啟成本藏在走路時間裡。
  `worker_warmup()` 必須跑**一次真的往返**(只 spawn 的話第一次實際辨識仍付冷啟，會吃掉整個窗)。
- **session 必須開新的**：同一 session 每多塞一張遊戲截圖，下一次就明顯變慢(6s → 11s 逾時)。
  `RESTART_AFTER=3`，且每次要解符文前 `worker_warmup(fresh=True)` 強制重開。
- **worker 的 cwd 必須是空目錄**(`%TEMP%\maple_rune`)：cwd 指到專案內時 claude 會載入
  `.claude/`、CLAUDE.md、git 狀態等專案 context，實測單次辨識從 ~3s 膨脹到 >10s。
  另加 `--strict-mcp-config` 不載入使用者的 MCP servers。

**真符文實測校準結果**(遊戲視窗 1368x800)：

| 模型 / 裁切 | 平均耗時 | 判讀正確 |
|---|---|---|
| haiku / 含公告文字 | 7.18s | 0/3 |
| haiku / 只留箭頭列 | 10.28s | 0/3 |
| sonnet / 含公告文字(2x 放大) | 4.77s | 0/3 |
| **sonnet / 只留箭頭列(2x)** | **4.73s** | **3/3** |
| **sonnet / 含公告文字(2x) + 修好提示詞** | **3.6~5.5s** | **4/4** |

- **sonnet 反而比 haiku 快**(瓶頸在往返不在模型)，且 haiku 判真符文只對 1/4 → 用 sonnet。
- ⚠️ **最大的坑是提示詞裡的顏色規則**：早期版本寫「橘紅三角頭指的方向 = 答案，綠色是尾巴」。
  但**箭頭顏色是循環動畫的** —— 同一顆符文先後出現過「橘頭綠尾」與「純綠 / 彩虹 / 紫」。
  規則對不上時模型會反覆推敲，**每次都拖到 9 秒逾時**。拿掉顏色、只講形狀
  (「忽略顏色，只看尖端朝哪」)後 → 3.6~5.5s、4/4 全對。
  **最初以為的「含公告文字就判錯 0/3」其實是放大倍率不足**，不是文字的錯。
- 同理，**箭頭定位不能靠色相**。`_locate_arrows()` 改成只看飽和度(冰原背景 S≈21，
  箭頭 S 167~243)。但橫帶本身 S 最高到 179，門檻設太低會被形態學閉運算黏成一個
  731x160 的大塊、把箭頭全吞掉。加兩道濾網：長寬比 0.6~1.7(箭頭≈正方 27x28；
  公告文字 90x23 是扁長條)+ 同列分群(4 支箭頭必在同一水平線，甩掉傷害數字 MISS)。
- **定位失敗不能放棄**：顏色動畫與遮擋會讓定位常常失敗。退路是固定框
  `FALLBACK_CROP`(0.22~0.42 x 0.25~0.75) 2 倍放大 ≈583 tokens —— 實測退路框
  照樣 4/4 全對。**定位器是最佳化，不是必要條件**。
  (退路框千萬不要用整個搜尋範圍 3 倍放大：那是 2871x720 ≈2756 tokens，實測 11 秒逾時。)
- 定位成功時 `_PAD=24` 留白 + `UPSCALE=3`：留白不足時兩端箭頭被切，**被切的那兩支就判錯，
  中間沒切的都對**。
- 回覆解析 `_parse_dirs` 容錯：先抓 JSON，失敗退回關鍵字掃描(模型偶爾吐無引號的 `{dirs:[...]}`)。
- **符文有冷卻**：按失敗後畫面顯示「暫時收不到效果(剩餘時間:N秒)」，沒等就再按也開不了。
  重試前 `RETRY_WAIT=6s`。**逾時會殺掉 worker，所以每輪重試前都要重新 `worker_warmup()`**，
  否則下一輪必定冷啟、必定再逾時。
- **實測時間預算**：按 B → 等 0.55s 渲染 → 辨識 3.6~5.5s → 按 4 鍵 ~0.5s ≈ **5~7s**，10 秒窗內。
  `ACTIVATE_KEY='b'` 已確認正確。

**導航段(實測通過)**：`/rune/calib` 掃描 dx=0~12 量出可用「環」：

| 距離 | 紫標可見 | 謎題開得了 |
|---|---|---|
| 0 | ✗ 被角色蓋住 | ✓ |
| 4~8 | ✓ | ✓ |
| 13 | ✓ | ✗ |

- `RADIUS_MIN=4`：再近角色會蓋住紫標。**注意 dx=0 時謎題照樣開得了，但紫標從小地圖消失，
  而 `_purple_gone()` 正是靠「紫標消失」判定解除成功 → 會誤報成功。這是安全問題不是效能問題。**
- `RADIUS_MAX=8`：外圈這個數字**不完全可信**，掃描時的「開不了」有一部分其實是冷卻造成的。
- 已在環內就**整段跳過導航**，省下走路時間。
- `SAME_LEVEL_DY=4`：紫標是符文圖示中心、角色點是身體中心，本就有固定偏差，實測 dy=2 與
  dy=4 都能正常按開。曾設成 2 → 把正常位置誤判成「不同平台」而卡死。
- ⚠️ **`navigator.Y_TOL=4` 會把 dy=4 當成「已到達」**，於是 `move_to` 回報成功但角色原地不動。
  沒對齊時把目標 y 往上超推 `Y_OVERSHOOT=5` 才會真的去爬繩。
- 端點 `/rune/probe`(只讀距離)、`/rune/calib`(掃描半徑)、`/rune/solve`(手動觸發完整流程)。

**⚠️ 符文不保證落在已記錄的平台上 → `_push_virtual_platform()`**：實測符文出現在 x=125，
而該層平台只記到 `xB=124`，差 1 格就掉進兩塊平台之間的空隙。`pathgraph.build_physics` 會把
目標當節點加進圖，但**目標不在任何平台區間內就沒有任何邊** → 直接回「無路徑」，
角色原地不動、看起來就像「遇到紫標只會暫停不會過去」。

規則(使用者定的)：**小地圖上有紫標，就等於那裡有平台**，不必逐一補平台記錄。做法是解符文
期間把 `{y:py, xA:px-RADIUS_MAX, xB:px+RADIUS_MAX}` 這塊虛擬平台插進 `navigator._terrain_fn`，
`build_physics` 會用二段跳(30px 內)自動把它接上旁邊的真實平台。**一定要在 `finally` 還原**，
否則虛擬平台殘留會影響之後的巡邏。驗算：

```
原本                   : (116,53)→(131,54) = 無路徑
加虛擬平台 y54 x117..133: (120,53)walk → (120,54)fall → (131,54)walk
```

**提示詞要加「不要推敲」**：遇到箭頭被完全遮住(實測是遊戲的 GUIDE 彈窗蓋住第 3 支)，模型會
反覆推敲直到 9 秒逾時，三輪白燒 75 秒。加上「完全看不到就回 unknown、立刻作答、不要推敲」後，
同一張圖 3.8~6.9s 就回答(`_parse_dirs` 只收 4 個方向詞，unknown 會讓長度 !=4 → 快速失敗重試)。

**端對端實測(三次皆成功)**：
1. `dy=5 環外 → 導航 → 落點 dist=6 dy=1 → 辨識 4.7s → 按鍵 → solved`，約 12 秒。
2. 角色 y=68、符文 y=29(**跨多層**) → 導航到 dist=7 dy=0 → 成功走到位。
3. `moved=false`(已在環內直接開謎題) → 辨識 5.1s → `[up,down,down,up]` → solved，約 10 秒。

三次的 `located` 都是 false(CV 定位失敗)，全靠**退路框**完成 —— 再次確認定位器只是最佳化。
- **接線**：`main._on_minimap_event` 紫標 → `rune.trigger_solve(data)`；沒接手(關閉/沒座標/
  正在解)回 False → 沿用 `navigator.pause_purple()`。端點 `/rune/{status,enable,line,warmup,detect,test}`，
  前端巡邏 tab 有開關 + 🔎測試辨識 + 🎯站符文上測試。

**兩條辨識線路可各自獨立開關**(`rune.set_lines` / `POST /rune/line?cv=&claude=`，前端兩個勾選框)：
- 1 線 = 純 CV(`rune_cv`，本機數毫秒)、2 線 = claude CLI(雲端 6~11s)。
- 兩條都開 = 原本的 auto：1 線先跑、要兩幀一致才採用，讀不到才退 2 線。只開一條就只走那條、不銜接。
- **兩條都關會被擋下**：那不等於關閉功能，而是留著「自動解除：開」的假象 —— 紫標照樣被接手、
  角色照樣走過去開謎題，然後辨識必定回空、白燒一次符文冷卻。要全關請關 `/rune/enable`。
- 2 線關掉時 `worker_warmup()` 直接略過(不為用不到的後端付 5 秒冷啟)，並停掉常駐子行程。
- `MAPLE_RUNE_LINE`(auto|cv|claude)退化成**開機預設**，執行中以 `set_lines` 為準；不持久化，
  跟 `/rune/enable` 一致。

**失敗要能自己重試 → `_solve_flow` 外層重試迴圈**：拆成 `_attempt()`(單次完整嘗試,回 bool)
+ `_solve_flow()`(外層重試)。**這層是必要的**，因為兩個機制會讓「一次失敗 = 永久卡死」：
- minimap 的紫標 hook 是**邊緣觸發**(前一幀沒紫標→這幀有)，紫標一直在就不會再觸發。
- `navigator._patrol_run` 每輪有兜底：只要小地圖有紫標就停巡邏。

所以失敗後就算接回巡邏，也會被兜底立刻停掉，且不會再觸發解符文 → 角色杵在原地。
現行收場：成功 → `_resume_fn()` 接回巡邏；`MAX_ATTEMPTS=3` 用完仍失敗 →
`navigator.pause_purple()` 回到原本的安全行為(暫停等人工；Telegram 通知 minimap 早就發過)。
另外每輪重試前重讀紫標座標，紫標已消失就直接視為完成(可能被別人解掉或自己消失)。
`_purple_gone()` 是「未知一律當沒消失」，寧可重試也不誤報成功。

**⚠️ 別用估來的半徑硬性放棄**：曾出現「巡邏遇到符文只停止+通知、看起來沒導航」的回報。
查 `/rune/status` 才發現**它有導航**(`moved=true`)，是被自己的守衛擋掉：

```
nav: target=[55,53] pos=[58,53] arrived=true     ← X_TOL=3,落在容差內就算到達
rune: dist=9  err="離紫標 9 格,超出互動半徑 8"    ← APPROACH_DX(6) + X_TOL(3) = 9 > RADIUS_MAX(8)
```

`APPROACH_DX + X_TOL` 本來就會超過 `RADIUS_MAX`，而 `RADIUS_MAX=8` 又是冷卻污染過的估計值。
改成**只警告、照樣按**(真按不到時 `_grab_crop` ~1s 就快速失敗，代價遠小於直接放棄)，
並在落點超出環外時用實際誤差反推目標**補正走一次**。虛擬平台要罩住整個 `_attempt`
(含補正走位)，否則第二趟又會無路徑。

**失敗要留證據 → `rune_pressed.png`**：真的按下方向鍵的那張圖會另存一份。`rune_shot.png`
會被後續重試覆蓋，失敗後根本無從判斷「是判讀錯還是按鍵沒生效」。
另外驗證改成**輪詢 `VERIFY_WAIT=3.5s`** 而不是按完 1.2s 只看一次 —— 紫標不會在按完的瞬間
就從小地圖消失，看一次可能誤判成失敗，還白白吃一次符文冷卻。

**實測(自動觸發，含重試)**：第 1 輪辨識逾時(11s，API 偶發慢) → 等 6s 冷卻 → 重新預熱 →
第 2 輪 4.83s 認出 `[up,right,right,left]` → 按鍵 → 紫標消失 `solved=true`，約 30 秒。
事後用 `rune_pressed.png` 查證判讀 4/4 正確。

### ❌ 符文箭頭判向 CNN 化嘗試 —— 驗收沒過，已預設關閉（2026-08-16 更正：前提錯了，方向也錯了）

**⚠️ 這整節先前的結論是錯的，而且錯在最根本的前提上。原文寫「膠囊定位零誤差」，
這句話直接誤導了後面八個任務的方向。留下這段是為了讓人不要重蹈——不是每次都對，
記錯的地方要跟記對的地方一樣清楚。**

**先講最重要的一句話：判向早就解決了（真值框下 99.5%），定位才是唯一瓶頸（可用率只有
54.1%）。整條線路實測 41% ≈ 定位 54.1% × 判向 97.3%。** 先前整個 CNN 專案是拿去改善
判向，而判向本來就沒壞——方向從根本上就錯了。

#### 錯在哪：把「框的尺寸」誤讀成「框的位置」

原文說的「膠囊定位零誤差」，量的其實是 `find_capsule()` 回傳框的**尺寸**恆為
329×81——這是模板比對的固有性質：不管有沒有貼中膠囊，回傳框的大小都一樣，所以
「尺寸零誤差」100% 成立，卻什麼也沒證明。真正該量的是框的**位置**有沒有對準膠囊，
這件事當時完全沒做。模板比對貼偏了，會回傳一個尺寸完全正確、但位置在錯的地方的框，
產生的訊號跟貼對時一模一樣、看不出差別——這就是當時被騙過去的原因。

而訓練 CNN 用的裁切，正是拿 `find_capsule()` 定位膠囊、四等分切出四張箭頭圖。定位一旦
偏了，裁切下來的圖就可能是背景、隔壁箭頭、或半張圖，配上的卻是「正確」的標籤——等於
拿背景圖配正確答案在訓練。CNN 學不會，不是模型能力不夠，是資料本身就是錯的。

#### 1. 定位才是瓶頸

把 `find_capsule()` 的框拿去跟人工驗證過的真值框（`rune_dataset/box_truth.json`）比對，
351 筆可對照（另外 13 筆完全找不到框）：

| 誤差 | 筆數 | 佔比 |
|---|---|---|
| ≤5px 幾乎精準 | 105 | 29.9% |
| 6~20px 同格內，判向不受影響 | 85 | 24.2% |
| 21~60px 開始切到隔壁格 | 36 | 10.3% |
| >60px 完全跑掉 | 125 | 35.6% |

p25=5、p50=13、p75=119、p90=182 px——分佈中段就已經跳得很開，代表定位失敗不是少數
極端值拖累，而是系統性的。**定位可用率（誤差 ≤20px）只有 190/351 = 54.1%。**

#### 2. 判向早就解決了

用**真值框**裁切（不是 `find_capsule()` 的框），跑現有的色度分割 + 模板判向
（`_chroma_map`/`_seg`/`_direction_tpl`，完全不用 ML），全部 364 筆：單支
**97.3%**、四支全對 90.1%。在按 ts 切出的驗證集上更高：`min_blob≥10` 時
**99.5%**、`≥20` 時 99.4%。這條路徑從頭到尾沒有機器學習，純幾何 + 色度距離，
數毫秒內出結果。

#### 3. CNN 即使餵乾淨資料也打不過模板判向

修正裁切、改用真值框重訓之後（同一個驗證集、公平比較）：

| min_blob | 現有模板判向(無 ML) | CNN |
|---|---|---|
| ≥0 | 98.6% | 79.6% |
| ≥10 | 99.5% | 92.9% |
| ≥20 | 99.4% | 95.2% |

**CNN 在每個設定上都輸現有模板判向 4~19 個百分點。判向這件事不該用 ML 做**——
手寫的色度分割 + 模板比對已經是更準、更快、更好除錯的解法。

#### 4. 但根因診斷的方向是對的——只是診斷錯了問題

用壞裁切訓出來的模型：訓練集 75.8% / 驗證 78.2%（訓練 < 驗證，這是欠擬合的訊號——
連自己看過的資料都學不會，說明資料本身有問題，不是模型不夠強）。改用真值框
（364 筆真值）重訓：

| min_blob | 樣本數 | 訓練 | 驗證 | 純方向(對照) |
|---|---|---|---|---|
| 0 | 364 | 77.2% | 82.9% | 79.6% |
| 10 | 246 | 90.2% | 92.6% | 92.9% |
| 20 | 187 | 94.1% | 94.8% | 95.2% |

訓練集正確率從 75.8% 一路升到 94.1%，欠擬合的訊號消失了。**餵對圖，模型就學得起來，
先前學不會純粹是裁切壞了，不是模型能力、資料量或信心度校準的問題**（第一輪只有 179
筆真值時是訓練 92.6% / 驗證 74.8%，18pp 過擬合；真值補到 364 筆後這個落差也消失了，
說明資料量確實是有效槓桿，但前提是資料本身要乾淨）。

#### 5. 膠囊框真值已重新產生

`rune_dataset/box_truth.json` 已從 179 筆補到 **364/365（99.7%）**，全部靠遊戲內驗證
或人工判讀標定，不是模型猜的。品質分層（用真值框跑判向的四支全對率）：`blob_n≥0` →
90.1%、`≥10` → 93.5%、`≥20` → 96.3%、`≥80` → 100%。`blob_n` 可以直接當可信度權重用。

#### 6. 判向對位置誤差的容忍度（下一版偵測器的規格）

| 位置抖動 | 單支正確率 |
|---|---|
| ±0 | 98.4% |
| ±2 | 98.2% |
| ±4 | 97.4% |
| ±6 | 96.1% |
| ±8 | 94.4% |
| ±12 | 88.5% |
| ±16 | 80.5% |
| ±20 | 73.9% |

**偵測器只要把箭頭位置抓到 ±6px 內，判向就有 96%。** 這條表格是量出來的容忍度曲線，
不是猜的，未來設計偵測器可以直接拿這張表當驗收線。

#### 7. 修正後的方向：不偵測膠囊，直接偵測箭頭

膠囊框只在**離線推導訓練標籤**時用得到——364 個真值框可以展開成 1456 個已知箭頭位置，
拿來訓練或校準一個箭頭偵測器。**推論（正式路徑）時完全不需要偵測膠囊**，直接找 4 支
箭頭的位置，再交給現有的色度分割 + 模板判向。

一個容易漏掉的細節：判向器要用一片以背景為主的區域估背景色度；單獨一格自己估背景，
正確率會從 98.4% 掉到 92.2%。但這不需要另外偵測膠囊來解決——4 支箭頭都找到之後，
它們圍出來的範圍本身就是那塊背景區域，直接拿來估色度即可。

#### 原文裡仍然正確、沒有被推翻的部分

- **CNN 目前預設關閉**：`server/rune_nn.py` 的 `ENABLED` 預設 `False`（讀環境變數
  `MAPLE_RUNE_NN`，設 `1` 才開），`read_dirs` 正式路徑維持走 `_read_dirs_chroma`
  （現行色度分割）。已用 704 次比對（352 張正樣本 × strict True/False）逐位元驗證過：
  CNN 關閉後的 `read_dirs` 輸出與接上 CNN 之前（commit `9bdc5a8`）完全一致，0 個不
  一致。退回舊路徑的機制本身沒有問題，可以放心維持關閉。
- **前處理只能有一份**：`server/rune_nn.py` 的 `preprocess`/`preprocess_batch` 由
  train/serve 共用同一份實作，不是各寫一份——這條原則依然成立，避免訓練與線上推論
  之間出現前處理不一致。
- **rot90 標籤增強依賴固定順序**：`rune_nn.py` 的方向類別順序對齊 `rune_cv._TPL_DIRS`
  （順時針），rot90 資料增強直接依賴這個順序做標籤旋轉，不能重排——這個約束依然有效，
  之後不管是重訓判向模型還是訓練新的箭頭偵測器，都要延續這個順序。

原文裡「被擋下的樣本會退給 2 線（claude CLI）處理，不是解不開」這個機制描述仍然正確，
但建立在它之上的「混合式設計：CNN 接手舊閘門擋下的樣本」已經沒有意義——現在已知判向
不是瓶頸，把 CNN 用在判向上是解決一個本來就沒壞的問題。CNN（或任何模型）真正該用的
地方，是第 7 點講的箭頭位置偵測，不是方向分類。

### ⚠️ `pathgraph.build_physics(jump_up=11)` 高估二段跳 → 已改 8

新地圖(186x68)導航一直失敗，症狀是「反覆規劃二段跳上去但實際跳不上」。用 `nav_moves.jsonl`
7892 筆實測統計，答案很清楚：

| 想上升 | 成功/總數 | 成功率 |
|---|---|---|
| 8 格 | 404/404 | **100%** |
| **11 格** | **1/8** | **12.5%** |
| 15 格 | 0/10 | 0% |
| 23 格 | 0/37 | 0% |

二段跳真實上限在 8~11 之間，`jump_up=11` 剛好把 11 當成可行。該地圖 y=51→y=40 正好差 11，
於是整條路徑都規劃成二段跳、次次失敗(1/8 那次應是落點誤差湊巧)。改成 `jump_up=8` 後：

```
jump_up=11: (91,51)walk → (121,40)jump   ← 只有 12.5% 會成功
jump_up= 8: (110,51)walk → (110,40)rope  ← 改走繩索
```

該地圖 `ropes: []` 沒記錄繩索也沒關係 —— navigator 的 `_rope_to` 會現場按 C 測、
沿重疊區 ±4/8/12 掃描找繩索。**實測從下層 dy=11 一次就上去並解除成功(約 15 秒)。**

### 其他修正

- **`ask()` 沒檢查 `is_error` → 沉默失敗**。CLI 層級錯誤(認證過期/額度用盡/Read 被拒)也是
  `type=result`，但 `is_error=true` 且 `result` 是錯誤訊息。不檢查就被當成正常回覆，
  解析不出方向 → `dirs=[] err=""`，外層只看到「辨識失敗」完全不知道真因
  (實測 1.2s 就回來，正常要 4~6s)。已加檢查，並在「有回覆但抽不出方向」時回報原始文字。
- **角色蓋住紫標不再整輪放棄**：實測落點 `dist=4` 就會蓋住，而 `RADIUS_MIN` 正好是 4，
  邊界值直接踩雷。改成先往外退到環中間再試，退完仍看不見才放棄；`RADIUS_MIN` 收到 5。
- **`_platform_of()` / `_same_platform()`**：比對【區間 + 高度】而非只比高度。該地圖同時有
  y=28/28/29 三塊平台，只比高度會把隔壁那塊當成同一層。

**尚未驗證**：解完自動接回巡邏(`resume=1`)還沒實際看過(測試都用 `resume=0`)。

## ✅ 符文判向改用模型類別，取代色度分割（2026-08-17，四支全對 62.6% → 85.2%）

使用者回報靜態判向成功率不到七成，複雜背景、深藍色系箭頭常判錯。量測證實了體感：
色度分割在 115 筆**未參與訓練**的樣本上，四支全對只有 **62.6%**。

### 根因不是模型不會判，是 production 沒在用模型判

RT-DETR 從第一天就在輸出方向類別，但 `detect_arrows()` **只回框、把類別丟掉**，方向另外
交給 `_read_dirs_detr` 的色度分割去做。而 `_chroma_map` 靠「離背景中位色度的距離」切出
箭頭形狀 —— 箭頭與背景色度接近時（深藍箭頭配藍背景）分割就殘缺，模板再好也救不回。

同一組框、只換判向方式（115 筆未參與訓練）：

| 判向方式 | 單支正確 | 四支全對 |
|---|---|---|
| 色度分割（原） | 86.5% | 62.6% |
| **模型類別（新）** | **95.0%** | **85.2%** |

### 改動

- `rune_detr`：`_postprocess` 一併回傳類別索引；新增 `detect_labeled()` 回 `(boxes, labels)`，
  類別名讀 checkpoint 的 `id2label`，1/4/5 類模型都適用。`detect_arrows()` 變薄包裝，
  回傳形狀不變，既有呼叫端零影響。
- `rune_cv._read_dirs_detr(frame, boxes, labels=None)`：**模型給得出方向的箭頭完全不跑
  色度分割**（不是算完再覆寫，`_chroma_map` 也改成延遲計算）。逐支決定而非整組切換 ——
  模型對某一支沒把握時，只有那一支落回色度。`rot` 不落回色度，留 `None` 給旋轉分支。
- `rune_viz`：預覽同步改走 `detect_labeled`，避免預覽方向與實際按鍵不一致。

### 這一輪最貴的教訓：資料集標註錯了三層

先訓練了一版「經典 246 + 新採集 297」的混合模型，subagent 回報新款準確率 94.9%，
走 production 入口實測是 **1.8%** —— 模型在**自己的訓練資料**上幾乎偵測不到東西。
逐層拆下去發現標註有三個獨立的錯：

1. **漏標 45%**：標註腳本先用 `rune_wheel.angle_of()` 量角度再吸附成方向，而
   `angle_of` 回 `None` 的箭頭**連框一起被丟掉**。新款 1188 支只標了 649 支，
   每筆平均剩 2 支。**畫面上有箭頭卻沒標，等於明確教偵測器「這裡是背景」** ——
   這才是分類信心從 0.97 崩到 0.06、被 `min_score` 閘門全濾掉的主因。
2. **方向標籤無意義**：新款全是旋轉款，吸附的是「旋轉中的瞬間角度」，不是謎題答案。
   同一個外觀會拿到互相矛盾的類別（實測兩支視覺上都指左上 135°，一標 `up` 一標 `left`）。
   旋轉款的答案是箭頭經過答案方向時約 67ms 的角速度反轉，**單幀原理上看不到**。
3. **`angle_of` 綠色遮罩缺上界**：`green = hh >= 45` 沒有上界，而整體遮罩含 `H>165`，
   洋紅像素（紅箭頭頭部在高亮度下的色相）被**同時算進紅與綠**，把綠尾重心往紅頭拉、
   兩心距離縮到門檻以下而回 `None`。1188 支有 590 支（50%）因此讀不出角度，
   修正後降到 41%。**這支函式是 production 的 `rune_wheel` 在用的同一支**，
   所以這個修正同時改善線上的旋轉解謎。

剩下的 41% 是**配色本身跑遍整個色環**（藍→綠→洋紅、紫→洋紅、綠→青都有），
那是 `angle_of`「紅頭綠尾」假設的結構性限制 —— 旋轉判向要根治，一樣得交給模型，
但需要旋轉款的角度真值，目前沒有。

### 5 類模型（方向 + rot）已訓練，但**不換**

`tools/rune_1cls_dataset_build.py` 產生 1 類 / 5 類標註（新款用 `meta.json` 的完整四框，
繞過 `angle_of`）；`train_rune_detr.py` 加 `--single-class` / `--with-rot` / `--split`。
held-out 結果：方向 99.4%、`rot` 零交叉污染（先前擔心「模型學到冰背景=旋轉」**沒有發生**），
但**定位退步**：靜態選得出 4 支 86.3%，舊模型是 98.0%。定位是基礎，所以線上維持既有權重，
5 類模型留在 `models/rune_detr_5cls/`。退步原因未查（懷疑新款偽標框較鬆，
或 1188 個 `rot` 框佔比過半壓過經典款）。

### ⚠️ 評估一定要走 production 入口

同一個坑這輪踩了三次，最誇張一次是 **94.9% vs 1.8%**。評估腳本只要自己重刻「取分數前
4 名」之類的簡化版，就會跳過 production 的閘門（幾何選擇、`min_score=0.015`、
`min_goodness=-10`），量到的是「原始偵測排序」而不是系統行為。要比較不同模型請用
`MAPLE_RUNE_DETR_DIR` 環境變數覆寫模型路徑，**不要另外寫一條推論路徑**。

### 待補：困難樣本沒有真值

經典資料集太乾淨（色度與模型都 98~99%，分不出高下），真正會出錯的「箭頭與背景色度接近 +
複雜背景多怪物」**沒有帶方向真值的樣本**（`rune_collect/` 300 筆全是旋轉款）。
唯一可信的真值來源是 `purple_gone`（解謎成功即代表按對），值得接一個「解謎成功時自動存下
該幀與確認過的答案」的機制持續累積。

## 下一步優先序
1. 符文自動解除:讓紫標事件**自己觸發**跑一輪(開總開關 + 巡邏中遇到真符文)，並驗證解完接回巡邏。
2. 用 `purple_gone` 當真值累積困難靜態樣本（複雜背景／深藍色系），現有資料集分不出高下。
3. 小地圖偵測尺寸穩定化(根治座標錯位)。
4. 巡邏參數/落點微調(nav_moves.jsonl 累積數據)。
